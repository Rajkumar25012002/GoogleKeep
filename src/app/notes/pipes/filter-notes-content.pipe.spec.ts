import { FilterNotesContentPipe } from './filter-notes-content.pipe';

describe('FilterNotesContentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotesContentPipe();
    expect(pipe).toBeTruthy();
  });
});
