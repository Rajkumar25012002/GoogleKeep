import { FilterNotesLabelPipe } from './filter-notes-label.pipe';

describe('FilterNotesLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotesLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
