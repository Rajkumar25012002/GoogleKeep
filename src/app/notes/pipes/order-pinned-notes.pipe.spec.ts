import { OrderPinnedNotesPipe } from './order-pinned-notes.pipe';

describe('OrderPinnedNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderPinnedNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
