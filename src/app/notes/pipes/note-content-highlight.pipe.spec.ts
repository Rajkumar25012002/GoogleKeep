import { NoteContentHighlightPipe } from './note-content-highlight.pipe';

describe('NoteContentHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteContentHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
