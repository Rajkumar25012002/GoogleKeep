import { LabelExtractorDirective } from './label-extractor.directive';

describe('LabelExtractorDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelExtractorDirective();
    expect(directive).toBeTruthy();
  });
});
